import { useEffect } from 'react';
import QuizService from '../Services/QuizService';
export const isAdmin = () => {
    useEffect(()=>{
        const getUser = async () => {

        }
    },[])       
}